# 第 3 周理论速查：pytest

pytest = 用 Python 做 Postman 里那种「发请求 + 自动检查」。  
真正发 HTTP 的仍是 `requests`，但用例里调用的是 `common/http_client.py` 的封装。  
`assert` 负责断言，pytest 负责找到并执行 `test_` 用例。

本周练习站仍是 `https://httpbin.org`。目录：`week03-pytest/`。  
跑命令前先 `cd` 到该目录，用虚拟环境里的 Python：

```powershell
.\.venv\Scripts\python.exe -m pytest tests -v
```

`-v` 打印每条用例名字。改完代码必须 **Ctrl+S 再跑**，pytest 读的是磁盘文件。

---

## 1. 和 Postman 对照

| Postman | pytest |
|---|---|
| 方法 GET/POST + URL | `get(url)` / `post_json(url, json)`（封装在 `common/`） |
| Body raw + JSON | `json={...}`（自动带 Content-Type） |
| 状态码是 200 | `assert response.status_code == 200` |
| 检查 JSON 字段 | `assert body["json"]["username"] == "admin"` |
| Test Results 绿/红 | PASSED / FAILED |
| `{{baseUrl}}` | fixture `base_url`（从 `config/env.yaml` 读） |
| Collection | `tests/` 目录 |

Postman 的 `pm.test` 是 JavaScript，不必当主语言。测开主技能是 pytest。

---

## 2. 收集规则

- 文件名：`test_*.py` 或 `*_test.py`  
- 函数名：`test_` 开头  
- 指定文件：`pytest tests\test_httpbin_get.py -v`  
- 整个目录：`pytest tests -v`（本周实际是 7 条：GET + POST + 参数化 3 轮 + skip + xfail）

终端里打印的是 **函数名**，不是文件名。文件叫 `post`、函数仍叫 `get_foo`，看起来会像在跑 GET。

`collected N items`：找到了 N 条。`[50%]` / `[100%]` 是进度。

---

## 3. 本周写过的两条接口

**GET** `tests/test_httpbin_get.py`  
`GET {base_url}/get?foo=1`  
断言 200，且 `body["args"]["foo"] == "1"`。打了 `@pytest.mark.smoke`。

**POST** `tests/test_httpbin_post.py`  
`POST {base_url}/post`，`json={"username": "admin", "password": "123456"}`  
断言 200，且 `body["json"]["username"] == "admin"`。  
httpbin 把解析后的 JSON 放在响应的 `"json"` 字段里，所以是 `body["json"]["username"]`，不是 `body["username"]`。

`json=` 已带 `Content-Type: application/json`，不必再手写同一份 headers（写了也不错）。

真登录不要断言响应里还有密码；httpbin 是回显练习站，才可以断言 password。

---

## 4. 目录约定

| 目录 | 放什么 | 本周文件 |
|---|---|---|
| `config/` | 环境地址 | `env.yaml` → `base_url` |
| `data/` | 用例数据 | `post_username_cases.yaml` |
| `common/` | 发请求 | `http_client.py` 的 `get` / `post_json` |
| `tests/` | 断言 | GET、POST、标记演示 |

换环境改 YAML，不改用例。加一组 username 改 data，不改函数体。超时、失败打印以后只改 `common`。

**找项目根：** `tests/conftest.py` 在 `tests/` 里，上一级才是项目根。

```python
ROOT = Path(__file__).resolve().parents[1]
```

是 **`parents[1]`**（复数），不是 `parent[1]`。  
读 YAML 编码是 **`utf-8`**，不是 `urf-8`。

**导入 common：** 根目录 `pytest.ini` 要有 `pythonpath = .`，并在项目根跑 pytest。否则 `ModuleNotFoundError: common`。

用例写成 `from common.http_client import get`，不要再 `import requests`（封装文件内部才需要）。

---

## 5. fixture 和 conftest.py

**fixture：** 先准备好一样东西，再作为参数传给用例。像用例里的前置。

`tests/conftest.py` 会被 pytest 自动加载，不要写成 `test_` 用例。现在从配置读，不再写死 URL：

```python
from pathlib import Path
import pytest
import yaml

ROOT = Path(__file__).resolve().parents[1]

@pytest.fixture
def base_url():
    path = ROOT / "config" / "env.yaml"
    cfg = yaml.safe_load(path.read_text(encoding="utf-8"))
    return cfg["base_url"]
```

用例必须把 **同名参数** 写进括号：

```python
def test_httpbin_get_foo(base_url):
    url = f"{base_url}/get?foo=1"
```

只在函数体里写 `base_url`、括号里没有 → `NameError: name 'base_url' is not defined`。  
换环境只改 `config/env.yaml` 一处。

---

## 6. 参数化

同一套步骤、不同数据，用 `@pytest.mark.parametrize`，一个函数跑多轮。  
数据放在 `data/post_username_cases.yaml`，函数里读文件再传给 parametrize，不要把三组字典写死在测试文件里。

```python
def _username_cases():
    path = Path(__file__).resolve().parents[1] / "data" / "post_username_cases.yaml"
    rows = yaml.safe_load(path.read_text(encoding="utf-8"))
    return [(row["payload"], row["expected_username"]) for row in rows]

@pytest.mark.parametrize("payload,expected_username", _username_cases())
def test_httpbin_post_username_case(base_url, payload, expected_username):
    ...
    assert body["json"].get("username") == expected_username
```

终端里会变成 `[payload0-admin]`、`[payload1-user]`、`[payload2-None]`。

**缺字段必须用 `.get("username")`。**  
`body["json"]["username"]` 在没有这个键时是 `KeyError`，不是 `None`。

---

## 7. 两种失败不要混

| 现象 | 含义 | 怎么处理 |
|---|---|---|
| `AssertionError` / `KeyError` | 断言或代码问题 | 改用例 |
| `SSLError`、代理 `127.0.0.1:7897` | 网络/代理偶发 | 再跑一次；不是 fixture 写错 |

---

## 8. 标记

| 标记 | 终端 | 含义 |
|---|---|---|
| `@pytest.mark.skip(reason="...")` | SKIPPED | 这轮不执行 |
| `@pytest.mark.xfail(reason="...")` | XFAIL | 执行了，失败是预期的 |
| `@pytest.mark.smoke` | 配合 `-m smoke` | 自定义冒烟 |

`reason` 会出现在终端括号里。演示文件：`tests/test_marks.py`。不要给真实 GET 加 skip。

自定义标记要在根目录 `pytest.ini` 注册：

```ini
[pytest]
pythonpath = .
markers =
    smoke: 冒烟，主干通不通
```

只跑冒烟：

```powershell
.\.venv\Scripts\python.exe -m pytest tests -m smoke -v
```

应看到 `configfile: pytest.ini`，以及 `collected 7 items / 6 deselected / 1 selected`。

用 `@pytest.mark.xxx` 的文件必须 `import pytest`，否则收集阶段 `NameError: name 'pytest' is not defined`。

---

## 9. 失败时打印请求信息

```python
assert response.status_code == 200, (
    f"url={url} status={response.status_code} body={response.text}"
)
```

失败时先看 url、status、body，再看一长串库文件堆栈。

本周用把期望改成 201 来制造失败，看完必须改回 200。

---

## 10. 本周踩过的坑（复习优先看）

1. 改了函数名没保存，终端仍是旧名字。  
2. fixture / `pytest` 用了名字却没写参数或没 import。  
3. 缺字段用 `[]` 而不是 `.get`。  
4. SSL 红了当成用例写错。  
5. `Path.parent[1]` 应写成 `parents[1]`；编码写成 `urf-8`。  
6. 封装了 `post_json`，参数化那条仍直接 `requests.post` —— 拆目录要改干净。  
7. 用例文件顶部还留着没用的 `import requests`，应删掉。

---

## 11. 第 3 周过关对照

能：`pytest tests -v` 收集 7 条（5 passed + skip + xfail）；fixture 从 `config/env.yaml` 读；参数化数据在 `data/`；请求走 `common/`；缺字段用 `.get`；skip / xfail / `-m smoke`；失败 assert 带 url 和正文。

尚未做：自己的登录服务三条用例（被测服务搭起来之后）、Git / GitHub（第 4 周）。
